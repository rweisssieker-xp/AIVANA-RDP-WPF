<UserControl x:Class="Aivana_RDP_WPF.Views.Analytics.AnalyticsDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Aivana_RDP_WPF.Views.Analytics"
             xmlns:vm="clr-namespace:Aivana_RDP_WPF.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1200">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header Controls -->
        <Border Grid.Row="0" Background="#2c3e50" Padding="20,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBlock Grid.Column="0" Text="Intelligence Dashboard" 
                          FontSize="24" FontWeight="Bold" 
                          Foreground="White" VerticalAlignment="Center"/>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="0,0,20,0">
                    <ComboBox SelectedItem="{Binding SelectedPeriod}"
                             Width="120" Margin="5,0" VerticalAlignment="Center">
                        <ComboBoxItem>Hourly</ComboBoxItem>
                        <ComboBoxItem IsSelected="True">Daily</ComboBoxItem>
                        <ComboBoxItem>Weekly</ComboBoxItem>
                        <ComboBoxItem>Monthly</ComboBoxItem>
                    </ComboBox>
                    
                    <DatePicker SelectedDate="{Binding StartDate}" 
                              Width="120" Margin="5,0" VerticalAlignment="Center"/>
                    
                    <TextBlock Text="to" VerticalAlignment="Center" 
                              Foreground="White" Margin="5,0"/>
                    
                    <DatePicker SelectedDate="{Binding EndDate}" 
                              Width="120" Margin="5,0" VerticalAlignment="Center"/>
                </StackPanel>
                
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="Refresh" Command="{Binding RefreshAnalyticsCommand}"
                           Background="#3498db" Foreground="White"
                           Margin="5,0" Padding="15,5" BorderThickness="0"/>

                    <Button Content="HTML" Command="{Binding ExportAnalyticsCommand}"
                           CommandParameter="html"
                           Background="#95a5a6" Foreground="White"
                           Margin="5,0" Padding="12,5" BorderThickness="0"/>

                    <Button Content="PDF" Command="{Binding ExportAnalyticsCommand}"
                           CommandParameter="pdf"
                           Background="#95a5a6" Foreground="White"
                           Margin="5,0" Padding="12,5" BorderThickness="0"/>

                    <Button Content="CSV" Command="{Binding ExportAnalyticsCommand}"
                           CommandParameter="csv"
                           Background="#95a5a6" Foreground="White"
                           Margin="5,0" Padding="12,5" BorderThickness="0"/>

                    <Button Content="JSON" Command="{Binding ExportAnalyticsCommand}"
                           CommandParameter="json"
                           Background="#95a5a6" Foreground="White"
                           Margin="5,0" Padding="12,5" BorderThickness="0"/>

                    <Button Content="XML" Command="{Binding ExportAnalyticsCommand}"
                           CommandParameter="xml"
                           Background="#95a5a6" Foreground="White"
                           Margin="5,0" Padding="12,5" BorderThickness="0"/>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Loading Indicator -->
        <Grid Grid.Row="1" Background="#ecf0f1" 
             Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4"/>
                <TextBlock Text="Loading Analytics..." Margin="0,10,0,0" 
                          HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
        
        <!-- Main Content -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" 
                     Visibility="{Binding IsLoading, Converter={StaticResource InverseBoolToVisibilityConverter}}">
            <Grid Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <!-- Key Metrics Cards -->
                <UniformGrid Grid.Row="0" Columns="4" Margin="0,0,0,20">
                    <Border Background="White" CornerRadius="8" Padding="20" Margin="5"
                           Effect="{StaticResource CardShadow}">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="Total Connections" FontSize="12" Foreground="#7f8c8d"/>
                            <TextBlock Text="{Binding CurrentAnalytics.Statistics.TotalConnections}" 
                                      FontSize="24" FontWeight="Bold" Foreground="#2c3e50" 
                                      HorizontalAlignment="Center"/>
                            <TextBlock Text="Last 24 hours" FontSize="10" Foreground="#95a5a6"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Background="White" CornerRadius="8" Padding="20" Margin="5"
                           Effect="{StaticResource CardShadow}">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="Success Rate" FontSize="12" Foreground="#7f8c8d"/>
                            <TextBlock Text="{Binding CurrentAnalytics.Statistics.SuccessRate, StringFormat='{}{0:P1}'}" 
                                      FontSize="24" FontWeight="Bold" Foreground="#27ae60" 
                                      HorizontalAlignment="Center"/>
                            <TextBlock Text="Performance" FontSize="10" Foreground="#95a5a6"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Background="White" CornerRadius="8" Padding="20" Margin="5"
                           Effect="{StaticResource CardShadow}">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="Avg Latency" FontSize="12" Foreground="#7f8c8d"/>
                            <TextBlock Text="{Binding CurrentAnalytics.Performance.AverageLatency, StringFormat='{}{0:F1}ms'}" 
                                      FontSize="24" FontWeight="Bold" Foreground="#e74c3c" 
                                      HorizontalAlignment="Center"/>
                            <TextBlock Text="Network" FontSize="10" Foreground="#95a5a6"/>
                        </StackPanel>
                    </Border>
                    
                    <Border Background="White" CornerRadius="8" Padding="20" Margin="5"
                           Effect="{StaticResource CardShadow}">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="Security Risk" FontSize="12" Foreground="#7f8c8d"/>
                            <TextBlock Text="{Binding CurrentAnalytics.Security.OverallRisk}" 
                                      FontSize="24" FontWeight="Bold" Foreground="#f39c12" 
                                      HorizontalAlignment="Center"/>
                            <TextBlock Text="Assessment" FontSize="10" Foreground="#95a5a6"/>
                        </StackPanel>
                    </Border>
                </UniformGrid>
                
                <!-- Charts Section -->
                <Grid Grid.Row="1" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Connection Trends Chart -->
                    <Border Grid.Column="0" Background="White" 
                           CornerRadius="8" Padding="20" Margin="0,0,10,0"
                           Effect="{StaticResource CardShadow}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Text="Connection Trends" 
                                      FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                            
                            <Border Grid.Row="1" Background="#f8f9fa" CornerRadius="4" Height="200">
                                <TextBlock Text="📊 Chart Placeholder" 
                                          HorizontalAlignment="Center" VerticalAlignment="Center"
                                          Foreground="#95a5a6"/>
                            </Border>
                        </Grid>
                    </Border>
                    
                    <!-- Performance Metrics Chart -->
                    <Border Grid.Column="1" Background="White" 
                           CornerRadius="8" Padding="20" Margin="10,0,0,0"
                           Effect="{StaticResource CardShadow}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Text="Performance Metrics" 
                                      FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                            
                            <Border Grid.Row="1" Background="#f8f9fa" CornerRadius="4" Height="200">
                                <TextBlock Text="📈 Performance Chart" 
                                          HorizontalAlignment="Center" VerticalAlignment="Center"
                                          Foreground="#95a5a6"/>
                            </Border>
                        </Grid>
                    </Border>
                </Grid>
                
                <!-- Issues and Alerts Section -->
                <Grid Grid.Row="2" Margin="0,0,0,20">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Performance Issues -->
                    <Border Grid.Column="0" Background="White" 
                           CornerRadius="8" Padding="20" Margin="0,0,10,0"
                           Effect="{StaticResource CardShadow}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Text="Performance Issues" 
                                      FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                            
                            <ListBox Grid.Row="1" ItemsSource="{Binding PerformanceIssues}"
                                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                    BorderThickness="0" Background="Transparent" MaxHeight="200">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#fff3cd" CornerRadius="4" Padding="10" Margin="0,2">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="{Binding Description}" FontWeight="Bold"/>
                                                    <TextBlock Text="{Binding ImpactScore, StringFormat='Impact: {0:F1}'}" 
                                                              FontSize="10" Foreground="#6c757d"/>
                                                </StackPanel>
                                                
                                                <TextBlock Grid.Column="1" Text="{Binding Severity}" 
                                                          VerticalAlignment="Center" Margin="10,0,0,0"
                                                          Foreground="#856404"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </Border>

                    <!-- Remediation Suggestions -->
                    <Border Grid.Column="1" Background="White" 
                           CornerRadius="8" Padding="20" Margin="10,0,10,0"
                           Effect="{StaticResource CardShadow}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="Recommended Actions" 
                                      FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

                            <ListBox Grid.Row="1" ItemsSource="{Binding RemediationSuggestions}"
                                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                    BorderThickness="0" Background="Transparent" MaxHeight="200">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#e8f4fd" CornerRadius="4" Padding="10" Margin="0,2">
                                            <StackPanel>
                                                <TextBlock Text="{Binding Title}" FontWeight="Bold"/>
                                                <TextBlock Text="{Binding Description}" FontSize="11" TextWrapping="Wrap" Margin="0,2,0,0"/>
                                                <ItemsControl ItemsSource="{Binding Actions}" Margin="0,6,0,0">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate>
                                                            <TextBlock Text="{Binding}" FontSize="10" Foreground="#2c3e50" TextWrapping="Wrap"/>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </Border>
                    
                    <!-- Security Incidents -->
                    <Border Grid.Column="2" Background="White" 
                           CornerRadius="8" Padding="20" Margin="10,0,0,0"
                           Effect="{StaticResource CardShadow}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            
                            <TextBlock Grid.Row="0" Text="Security Incidents" 
                                      FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                            
                            <ListBox Grid.Row="1" ItemsSource="{Binding SecurityIncidents}"
                                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                    BorderThickness="0" Background="Transparent" MaxHeight="200">
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#f8d7da" CornerRadius="4" Padding="10" Margin="0,2">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="{Binding Description}" FontWeight="Bold"/>
                                                    <TextBlock Text="{Binding OccurredAt, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}" 
                                                              FontSize="10" Foreground="#6c757d"/>
                                                </StackPanel>
                                                
                                                <TextBlock Grid.Column="1" Text="{Binding Type}" 
                                                          VerticalAlignment="Center" Margin="10,0,0,0"
                                                          Foreground="#721c24"/>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </Grid>
                    </Border>
                </Grid>
                
                <!-- Usage Patterns -->
                <Border Grid.Row="3" Background="White" 
                       CornerRadius="8" Padding="20" Margin="0,0,0,20"
                       Effect="{StaticResource CardShadow}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Text="Usage Patterns" 
                                  FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                        
                        <ItemsControl Grid.Row="1" ItemsSource="{Binding UsagePatterns}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="3"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Background="#f8f9fa" CornerRadius="6" Padding="15" Margin="5">
                                        <StackPanel>
                                            <TextBlock Text="{Binding Type}" FontWeight="Bold" 
                                                      Foreground="#2c3e50"/>
                                            <TextBlock Text="{Binding Description}" 
                                                      TextWrapping="Wrap" Margin="0,5,0,0"/>
                                            <TextBlock Text="{Binding Confidence, StringFormat='Confidence: {0:P1}'}" 
                                                      FontSize="10" Foreground="#6c757d"/>
                                        </StackPanel>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Grid>
                </Border>
                
                <!-- Insights Section -->
                <Border Grid.Row="4" Background="White" 
                       CornerRadius="8" Padding="20"
                       Effect="{StaticResource CardShadow}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Text="Key Insights" 
                                  FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                        
                        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" MaxHeight="300">
                            <ItemsControl ItemsSource="{Binding Insights}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="#f8f9fa" CornerRadius="6" Padding="15" Margin="0,2">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                
                                                <Ellipse Grid.Column="0" Width="8" Height="8" 
                                                        Fill="#3498db" VerticalAlignment="Top" 
                                                        Margin="0,5,10,0"/>
                                                
                                                <StackPanel Grid.Column="1">
                                                    <TextBlock Text="{Binding Key}" FontWeight="Bold"/>
                                                    <TextBlock Text="{Binding Value}" 
                                                              TextWrapping="Wrap" Margin="0,2,0,0"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </ScrollViewer>
                    </Grid>
                </Border>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
