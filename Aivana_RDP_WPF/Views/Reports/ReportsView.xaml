<UserControl x:Class="Aivana_RDP_WPF.Views.Reports.ReportsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="1000">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Reports" FontSize="24" FontWeight="Bold" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <Button Content="Refresh" Padding="12,6" Command="{Binding RefreshCommand}"/>
            <Button Content="Open Folder" Margin="12,0,0,0" Padding="12,6" Command="{Binding OpenReportsFolderCommand}"/>
            <Button Content="Open" Margin="12,0,0,0" Padding="12,6" Command="{Binding OpenSelectedReportCommand}"/>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Reports}" SelectedItem="{Binding SelectedReport}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="File" Binding="{Binding FileName}" Width="3*"/>
                <DataGridTextColumn Header="Type" Binding="{Binding Extension}" Width="*"/>
                <DataGridTextColumn Header="Size" Binding="{Binding SizeBytes}" Width="*"/>
                <DataGridTextColumn Header="Modified (UTC)" Binding="{Binding LastModifiedUtc}" Width="2*"/>
                <DataGridTextColumn Header="Path" Binding="{Binding FullPath}" Width="4*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
