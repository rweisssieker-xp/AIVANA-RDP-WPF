<UserControl x:Class="Aivana_RDP_WPF.Views.Health.HealthOverviewView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Health Overview" FontSize="24" FontWeight="Bold" Margin="0,0,0,12"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,12">
            <ComboBox Width="320" ItemsSource="{Binding Profiles}" SelectedItem="{Binding SelectedProfile}" DisplayMemberPath="Name"/>
            <Button Content="Run Health Check" Margin="12,0,0,0" Padding="12,6" Command="{Binding RunHealthCheckCommand}"/>
        </StackPanel>

        <GroupBox Grid.Row="2" Header="Latest Result" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Resolved IP:"/>
                    <TextBlock Text="{Binding LastResult.ResolvedIp}" FontWeight="SemiBold" Margin="0,0,0,8"/>

                    <TextBlock Text="Ping:"/>
                    <TextBlock Text="{Binding LastResult.PingSuccess}" FontWeight="SemiBold" Margin="0,0,0,8"/>

                    <TextBlock Text="Ping Roundtrip (ms):"/>
                    <TextBlock Text="{Binding LastResult.PingRoundtripMs}" FontWeight="SemiBold" Margin="0,0,0,8"/>
                </StackPanel>
                <StackPanel Grid.Column="1">
                    <TextBlock Text="Port Open:"/>
                    <TextBlock Text="{Binding LastResult.PortOpen}" FontWeight="SemiBold" Margin="0,0,0,8"/>

                    <TextBlock Text="TCP Connect (ms):"/>
                    <TextBlock Text="{Binding LastResult.TcpConnectMs}" FontWeight="SemiBold" Margin="0,0,0,8"/>

                    <TextBlock Text="Error:"/>
                    <TextBlock Text="{Binding LastResult.ErrorMessage}" TextWrapping="Wrap" Margin="0,0,0,8"/>
                </StackPanel>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
