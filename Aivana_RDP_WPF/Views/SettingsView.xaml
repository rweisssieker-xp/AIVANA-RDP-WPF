<UserControl x:Class="Aivana_RDP_WPF.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="Settings" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
            
            <GroupBox Header="Appearance" Padding="12" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Theme" Margin="0,0,0,8"/>
                    <ComboBox SelectedItem="{Binding SelectedTheme}" Width="200" HorizontalAlignment="Left">
                        <ComboBoxItem Content="Light"/>
                        <ComboBoxItem Content="Dark"/>
                    </ComboBox>
                    <Button Content="Apply Theme" Margin="0,12,0,0" Padding="12,6"
                            Command="{Binding ChangeThemeCommand}"
                            CommandParameter="{Binding SelectedTheme}"/>
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </Grid>
</UserControl>

